isNegativeToInt x = 1 if x < 0 otherwise 0;

is42 x = (
    1 if x == 42 
    otherwise 0
);

isEven n = (1
    if (n / 2) * 2 == n
    otherwise
        1337 * 0
);

isOdd n = 1 if (n / 2) * 2 =/= n
    otherwise 1337 * 1;

main = isEven 42 == 1;
